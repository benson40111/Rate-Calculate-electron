{
  "name": "rate-calc",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=static/icon.icns --prune=true --out=release-builds"
  },
  "repository": "https://github.com",
  "keywords": [
    "Electron"
  ],
  "devDependencies": {
    "electron": "~1.7.8",
    "electron-packager": "^10.1.2"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "electron-reload": "^1.2.2",
    "nodemailer": "^4.4.1",
    "npm": "^5.6.0",
    "request": "^2.83.0"
  }
}
